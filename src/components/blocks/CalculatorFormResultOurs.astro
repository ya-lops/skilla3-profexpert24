---
import type { BrandData } from "@/types/brand";

import CtaPrice from "./CtaPrice.astro";

interface Props {
  brandData: BrandData;
}
const { brandData } = Astro.props;
---

<div class="relative h-full isolate" data-cmp="CalculatorFormResultOurs">
  <CtaPrice {brandData} />
</div>
